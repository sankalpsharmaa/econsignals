"""EconSignals local web app (FastAPI).

Serves the dashboard snapshot live from the database and records relevance
feedback. The public GitHub Pages site uses the static feed.json instead; this
backend is for local interactive use (live data, feedback that retrains the feed).
"""

# EconSignals dependencies
# Core (used by sensors/lenses)
certifi>=2023.0.0
numpy>=1.24.0

# Zotero personalization (embedding-based reranking via Ollama)
# Uses OLLAMA_HOST (default localhost:11434) + nomic-embed-text

# Testing
pytest>=7.0.0

# Optional extras (lazily imported; the install stays free/local without them)
# - scikit-learn: logistic learned-ranker (falls back to a numpy classifier)
# - transformers, adapters, torch: SPECTER2 embedding backend
#     (enable with ECONSIGNALS_EMBED_BACKEND=specter2)
# - LLM "why it matters": no extra package (stdlib urllib); enable with
#     ECONSIGNALS_RATIONALE=1 + ANTHROPIC_API_KEY
